{#
TODO: Exercise - Supplier Detail Template

This template displays details of a single supplier.
The controller passes a 'supplier' variable (SupplierTransfer).

Steps:
1. Extend the main page layout: {% extends template('page-layout-main') %}
2. Set the title using supplier.name
3. In the content block:
   - Add a breadcrumb: Suppliers > Supplier Name
   - Create a card with the supplier details
   - Display: ID, Name, Description, Email, Phone, Status
   - For status, show a badge: badge-success for active (status == 1), badge-secondary for inactive
   - Add a "Back to List" button linking to the supplier-index route

Hint: Use url() function for route generation: {{ url('supplier-index') }}
#}

{# TODO: Extend the main layout #}

{# TODO: Set the title using supplier name #}

{# TODO: Implement the content block with supplier details #}
