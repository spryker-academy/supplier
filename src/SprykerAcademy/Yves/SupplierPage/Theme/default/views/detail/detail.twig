{% extends template('page-layout-main') %}

{% block title %}{{ supplier.name | default('Supplier') }}{% endblock %}

{% block content %}
    <div class="container">
        <p><a href="{{ url('supplier-list') }}">&larr; {{ 'Back to list' | trans }}</a></p>

        {% if supplier.idSupplier is empty %}
            <div class="alert alert-warning">
                {{ 'Supplier not found.' | trans }}
            </div>
        {% else %}
            <div class="card">
                <div class="card-header">
                    <h1>{{ supplier.name }}</h1>
                </div>
                <div class="card-body">
                    <table class="table table-borderless">
                        <tbody>
                            <tr>
                                <th>{{ 'ID' | trans }}</th>
                                <td>{{ supplier.idSupplier }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'Description' | trans }}</th>
                                <td>{{ supplier.description | default('N/A') }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'Email' | trans }}</th>
                                <td>{{ supplier.email | default('N/A') }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'Phone' | trans }}</th>
                                <td>{{ supplier.phone | default('N/A') }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'Status' | trans }}</th>
                                <td>
                                    {% if supplier.status == 1 %}
                                        <span class="badge badge-success">{{ 'Active' | trans }}</span>
                                    {% else %}
                                        <span class="badge badge-secondary">{{ 'Inactive' | trans }}</span>
                                    {% endif %}
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        {% endif %}
    </div>
{% endblock %}
